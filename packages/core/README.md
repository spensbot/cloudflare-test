# Lib

Completely generic code to be reused in any app

I might turn this into a git submodule someday

## Requirements

- Minimal dependencies
- Local Imports: Must be relative and end in ".ts" to satisfy the deno runtime